# Dev DBs — pick one. Default plugin uses sqlite (no docker needed).
# Uncomment service you test, then set database.type in config.yml accordingly.
# Usage: docker compose up -d  →  docker compose down

services:
  # MySQL 8 — for database.type=mysql
  # mysql:
  #   image: mysql:8
  #   environment:
  #     MYSQL_ROOT_PASSWORD: change_me
  #     MYSQL_DATABASE: minecraft
  #     MYSQL_USER: mc
  #     MYSQL_PASSWORD: change_me
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 5s
  #     retries: 10

  # PostgreSQL 17 — for database.type=postgresql
  # postgres:
  #   image: postgres:17
  #   environment:
  #     POSTGRES_DB: minecraft
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: change_me
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pg_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     retries: 10

volumes:
  mysql_data:
  pg_data:
